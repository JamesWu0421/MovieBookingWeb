# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=register;encrypt=false;trustServerCertificate=true
spring.datasource.username=sa
spring.datasource.password=P@ssw0rd
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect

# JWT Configuration (預設值，可不設定)
# jwt.secret=MovieTicketSystemSecretKeyForJWTTokenGenerationMinimum256Bits
# jwt.expiration=86400000

# Logging
logging.level.org.springframework.web=INFO
logging.level.com.example.movie=DEBUG

# Development
spring.devtools.restart.enabled=true

# 啟用虛擬執行緒（可選，Java 21 特性）
spring.threads.virtual.enabled=true

# 圖片上傳配置
upload.path=${user.dir}/uploads
upload.base-url=http://localhost:8080
spring.servlet.multipart.max-file-size=2MB
spring.servlet.multipart.max-request-size=2MB